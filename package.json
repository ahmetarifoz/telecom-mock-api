{
  "name": "telecom-mock-api",
  "version": "1.0.0",
  "private": true,
  "description": "Telekom agent senaryolari icin bagimliliksiz multi-API mock server",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "swagger-ui-dist": "^5.29.5"
  }
}
